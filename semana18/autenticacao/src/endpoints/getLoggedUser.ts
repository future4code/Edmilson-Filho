import { Request, Response } from 'express';
import axios from 'axios';

export const getLoggedUser = async (id: string) => {
    try {
        const { Authorization } req.headers;
        
        if () {
            
        }
    } catch (err) {
        res.status(400).send({ message: err.message })
    }
}